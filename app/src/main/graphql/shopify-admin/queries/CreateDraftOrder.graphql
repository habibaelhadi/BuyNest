mutation CompleteDraftOrder($id: ID!) {
    draftOrderComplete(id: $id, paymentPending: false) {
        draftOrder {
            id
            name
            status
        }
        userErrors {
            field
            message
        }
    }
}
